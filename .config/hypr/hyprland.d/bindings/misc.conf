
bindr = CAPS, Caps_Lock, exec, eww update kb-caps-enabled=$(hyprctl devices -j | jq '.keyboards[] | select(.main == true) | .capsLock')
bindr = CAPS, Caps_Lock, exec, eww open kb-caps --duration 1s --arg monitor=$(hyprctl monitors -j | jq '.[] | select(.focused == true) | .id')

