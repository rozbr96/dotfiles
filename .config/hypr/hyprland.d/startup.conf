
exec = systemctl --user restart hypr-ipc
exec = systemctl --user restart eww
exec = systemctl --user restart network-monitor
exec = systemctl --user restart volume-monitor

exec-once = mako

exec-once = wl-paste --type text --watch cliphist store
exec-once = wl-paste --type image --watch cliphist store

exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP

exec-once = hyprctl dispatch swapactiveworkspaces 0 1
