
exec-once = mako
# exec-once = nm-applet
# exec-once = blueman-applet
# exec-once = $binpath/waybar.sh

exec-once = wl-paste --type text --watch cliphist store
exec-once = wl-paste --type image --watch cliphist store

exec = killall eww; eww daemon && eww open main-bar --arg orientation="horizontal" --arg monitor="1"

exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP

exec = hypr_ipc

exec = pkill -f "volume monitor"; volume monitor &

exec-once = hyprctl dispatch swapactiveworkspaces 0 1
