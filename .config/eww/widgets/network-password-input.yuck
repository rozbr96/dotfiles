
(defwidget network-password-input []
  (box
    :spacing 10
    :space-evenly false
    (label :text 'Password:')
    (input
      :class 'password-input'
      :password true
      :hexpand true
      :onaccept `networker connect_with_password "${networker-tmp-network}" "{}"`)))
