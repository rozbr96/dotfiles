
(include "widgets/icons/wifi-icon.yuck")

(defwidget wifi []
  (eventbox
    :cursor 'pointer'
    :onclick `${EWW_CMD} open networker --arg monitor=1 --arg networks="$(wifi list json)"`
    (wifi-icon)))
