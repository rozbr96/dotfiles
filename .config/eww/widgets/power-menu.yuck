
(include 'widgets/icons/power-menu.yuck')

(defwidget power-menu [monitor]
  (eventbox
    :cursor 'pointer'
    :onclick `${EWW_CMD} open power-menu --arg monitor=${monitor}`
    (power-menu-icon)))
