
(defvar kb-layout-name "")

(defpoll kb-layout-flag
  :initial '🔤'
  :interval '1s'
  `eww get kb-layout-flag`)

(defwidget kb-layout []
  (eventbox
    :tooltip kb-layout-name
    :onclick `hyprctl switchxkblayout all next`
    kb-layout-flag))
