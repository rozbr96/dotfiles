
ALIASES_FILE=~/.zsh/aliases
ASDF_FILE=/opt/asdf-vm/asdf.sh
ENV_FILE=~/.zsh/env

[[ -f $ALIASES_FILE ]] && source $ALIASES_FILE
[[ -f $ASDF_FILE ]] && . $ASDF_FILE
[[ -f $ENV_FILE ]] && source $ENV_FILE

